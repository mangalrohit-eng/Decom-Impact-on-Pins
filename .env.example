# Copy to .env.local for local development (never commit secrets).

# Required for live AI analysis + LLM email drafts on the dashboard.
OPENAI_API_KEY=

# Optional — defaults to gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini
